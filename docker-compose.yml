version: '3.7'
services:

  #Postgresql Service
  nj-db:
    image: postgres
    container_name: nathanjms-db
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: nathanjms_api
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - '54320:5432'
    networks:
      - nj-web

  #PHP Service
  nj-api:
    build:
      args:
        user: www
        uid: 1000
      context: .
      dockerfile: Dockerfile
    image: digitalocean.com/php
    container_name: nj-api
    restart: unless-stopped
    tty: true
    volumes:
      - ./:/var/www
    working_dir: /var/www
    networks:
      - nj-web

  #Nginx Service
  webserver:
    image: nginx:alpine
    container_name: webserver
    restart: unless-stopped
    tty: true
    ports:
      - "80:80"
    volumes:
      - ./:/var/www
      - ./nginx/conf.d/:/etc/nginx/conf.d/
    networks:
      - nj-web

#Docker Networks
networks:
  nj-web:
    driver: bridge
